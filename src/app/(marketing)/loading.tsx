import { Loader } from "@/components";
import React from "react"

const Loading = () => {
    return (
        <div className="z-[99] flex justify-center items-center h-screen">
            <Loader />
        </div>
    )
};

export default Loading
